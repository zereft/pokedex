import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    value: [],
}

export const pokemonSlice = createSlice({
    name: 'pokemon',
    initialState,
    reducers: {
        
    }
})