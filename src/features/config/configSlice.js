import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    value: []
}

export const configSlice = createSlice({
    name: 'config',
    initialState,
    reducers: {
        
    }
})